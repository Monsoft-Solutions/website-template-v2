/**
 * SeoMetadataConfig
 *
 * Defines defaults and templates used by metadata generation utilities.
 */

export type SeoMetadataConfig = {
    defaultTitle: string
    defaultDescription: string
    titleTemplate?: string
    defaultLocale?: string
}
