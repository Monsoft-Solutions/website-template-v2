/**
 * Service Category Type
 *
 * Categorizes services for filtering and organization.
 * Used to group similar services together on the listing page.
 *
 * @example
 * const category: ServiceCategory = 'development'
 */
export type ServiceCategory =
    | 'consulting'
    | 'development'
    | 'design'
    | 'marketing'
    | 'support'
    | 'other'
