export type BlogCategoryItem = {
    id: string
    name: string
    slug: string
    count?: number
}

export type BlogTagItem = {
    id: string
    name: string
    slug: string
    count?: number
}
